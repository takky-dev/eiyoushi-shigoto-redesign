# えいようしごと リニューアル案

[eiyoushi.shigotolink.com](https://eiyoushi.shigotolink.com/) を栄養士・管理栄養士専門の求人サイトへリニューアルするための実装案。3方向のデザインパターンをそれぞれ完全な静的ページとして実装しています。

## 見方

```
pattern-a/index.html   … 信頼のエディトリアル型（トップページ）
pattern-a/list.html    … 求人一覧（検索フィルター付き）
pattern-a/job.html     … 求人詳細
pattern-a/apply.html   … 求人について相談する（応募フォーム）
pattern-b/ , pattern-c/ … 同じ4ページ構成をB・Cのデザインで実装
assets/img/            … 3パターン共通で使う写真
```

各パターンとも、トップページ→求人一覧→求人詳細→相談フォーム、まで実際にリンクをたどって遷移できます。

ローカルで確認する場合は、このフォルダ直下で以下を実行してください。

```bash
python -m http.server 8080
```

その後 `http://localhost:8080/pattern-a/` のようにアクセスすると各パターンを確認できます（`file://` で直接開くと `fetch` や相対パスの一部が制限される場合があります）。

## 3パターンの方向性

競合サイト（レバウェル栄養士 / 栄養士転職ナビ / マイナビコメディカル / ジョブメドレー / 栄養士のお仕事）を実際に開き、CTAボタンやブランドカラーの `background-color` を `getComputedStyle` で直接取得して分析しました。わかったのは「緑+オレンジ」で業界が統一されているわけではなく、**各社がブランド基調色（ティール／ライムグリーン／青）をあえてズラし、CTAボタンだけ暖色にする**という共通ルールがあったことです。この分析を踏まえ、3パターンもそれぞれ別の基調色にしています。

| パターン | コンセプト | 基調色 | 参考にした実測配色 |
|---|---|---|---|
| A: 信頼のエディトリアル型 | 大きな写真1枚と余白で見せるミニマル構成 | ディープティール `#146356` | ジョブメドレー（実測 `#0F766E`） |
| B: あたたかいエージェント型 | 丸ゴシック・チェックリスト・2択CTAの王道パターン | ライムグリーン `#6F9124` + オレンジ `#E2582E` | 栄養士転職ナビ（実測 `#9CC42F` / `#FD8E3C`） |
| C: 栄養士ならではの遊び心型 | 「求人成分表示」という栄養成分表示のパロディで独自性を作る | ハーブグリーン `#2F6B4F` + オレンジ `#E08A2E`、寒色寄りのセージホワイト背景 | オリジナル |

3パターンとも、掲載する求人・カテゴリ・エリア・条件などのコンテンツは同一にしてあります。デザインの違いだけで比較できるようにするためです。

## 求人一覧・求人詳細・応募導線について

見た目だけでなく「求人を探しやすいか」「応募しやすいか」も、栄養士転職ナビの実サイトを操作して確認したうえで実装しています。

- **求人一覧**（`list.html`）: キーワード・エリア・雇用形態に加えて、職種／施設種別／こだわり条件をチェックボックスのピルで絞り込める形にしました。競合サイトは「こだわり条件」だけで10〜15項目ある本格的な絞り込みUIを持っており、それを簡略化して再現しています。件数表示＋ページネーションも設置済みです。
- **求人詳細**（`job.html`）: 競合の求人詳細ページを実際に開いて確認したところ、共通して次の構造になっていました。
  1. 給与・エリア・キャッチコピーをまとめたハイライトボックス
  2. **「キープする」（保存）＋「求人について話を聞いてみる（無料）」の2階層CTA**
  3. CTAの直下に「※お問い合わせいただいた求人は、そのまま応募へ進むわけではありません」という一文
  4. 業務内容・給与・勤務時間・休日・福利厚生などの詳細スペック表
  5. モバイルでは画面下部に同じCTAが常に固定表示される（スクロールしても消えない）

  これは、これらのサイトが「求人を直接応募できる求人サイト」ではなく「アドバイザーが仲介する転職エージェント」であることを踏まえた設計です。**「応募」ではなく「相談」を前面に出すことで心理的ハードルを下げる**という、競合が共通して採用している考え方をそのまま踏襲しています。同じ理由で、本実装でも直接応募フォームではなく相談フォームにしています。
- **相談フォーム**（`apply.html`）: 項目はお名前・フリガナ・電話番号・メールアドレス・保有資格・連絡希望時間帯・備考のみ（1分程度で入力できる分量）。冒頭に「応募が確定するわけではない」旨の注記を置き、心理的ハードルを下げています。項目数を絞ったのは、競合サイトの求人詳細ページ下部に同種の注記があり、詳細な項目（職務経歴など）は初回フォームでは求めていなかったためです。

なお、求人一覧の「もっと見る」「求人の詳細を見る」は、デモの都合上すべて同じ `job.html` にリンクしています（実装時は求人IDでURLを分岐させてください）。

## 写真について

`assets/img/` の4枚はGemini生成の日本人モデル画像（ユーザー提供）です。

| ファイル | 用途 |
|---|---|
| `hero-consult.jpg` | パターンA・Cのヒーロー写真、パターンCの「求人成分表示」タグの背景 |
| `cat-hospital.jpg` | 病院・クリニックのカテゴリ写真、パターンBのヒーロー写真 |
| `cat-eldercare.jpg` | 高齢者・介護施設のカテゴリ写真 |
| `cat-schoolmeal.jpg` | 学校給食のカテゴリ写真 |

**未対応**: 「保育園・幼稚園」カテゴリの写真がまだありません。現状はアイコン表示のままです。以下のプロンプトで同じ縮尺（正方形構図・明るい自然光）の画像を追加生成し、`assets/img/cat-nursery.jpg` として配置後、各 `index.html` の該当箇所（`cat-icon` の中身）を他カテゴリと同じ形で差し替えてください。

> 日本の保育園の給食室で、白い調理服とマスクを着けた日本人栄養士が、彩り豊かな給食のトレーを並べている。明るく柔らかい自然光、清潔感のある雰囲気。正方形構図。

また、Gemini生成画像特有のクセとして、PC画面の文字や本の背表紙など細部の文字が崩れて見える箇所があります。現状の縮尺（サムネイル〜ヒーロー程度）では問題ありませんが、画像をトリミングして大きく見せる用途は避けてください。

## 実装時の注意点

- すべて素のHTML/CSSで、フレームワーク非依存です。既存の `eiyoushi.shigotolink.com` の実装スタックに合わせて、コンポーネント単位で移植してください。
- 検索フォーム・「求人を検索」ボタン・各種CTAは見た目のみで、実際の検索・送信処理は未実装です。
- 掲載している求人4件・統計数値（掲載求人数など）はすべて仮のサンプルです。実データに差し替えてください。
- モバイル幅（720px以下）ではハンバーガーメニューと画面下部固定のCTAバーに切り替わります。
- Googleフォント（Zen Kaku Gothic New / Zen Maru Gothic / Kaisei Decol / IBM Plex Mono）を使用しています。社内配信ポリシー上セルフホストが必要な場合は `<link>` タグをローカルフォントに差し替えてください。

## 元になった比較検討資料

このリポジトリの前段階として作成した、3パターンの見比べ用ドキュメント（写真・配色の検討過程を含む）:
https://claude.ai/code/artifact/b814ce6c-fd8a-47b2-ace3-cf0dbe4c9cf1
